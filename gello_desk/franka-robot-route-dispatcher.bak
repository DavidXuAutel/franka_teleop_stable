#!/bin/bash
ROBOT="10.229.20.91"
WIRED="eno1"
WIRED_IP="10.229.20.125"
IFACE="$1"
ACTION="$2"

if [ "$ACTION" = "up" ] && [ "$IFACE" = "$WIRED" ]; then
  ip route replace "${ROBOT}/32" dev "${WIRED}" src "${WIRED_IP}" metric 10 2>/dev/null || true
  ip route del 10.229.66.91/32 2>/dev/null || true
fi
